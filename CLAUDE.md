<!-- The project's agent rules live in AGENTS.md (the cross-tool standard that
     opencode/Codex also read). Keep them there, single source of truth; this
     file just imports them so Claude Code picks them up. Add Claude-Code-only
     notes below the import if any ever arise. -->
@AGENTS.md
